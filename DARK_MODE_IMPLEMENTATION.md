# תמיכה במצב תצוגה כהה/בהיר

## סיכום השינויים

המערכת עודכנה לתמוך במצב תצוגה כהה ובהיר עם מעבר חלק ביניהם.

## מה שונה?

### 1. משתני CSS גלובליים (`styles.scss`)
- הוספו משתני CSS למצב בהיר (ברירת מחדל) ולמצב כהה
- צבעי טקסט, רקע, גבולות וטבלאות מותאמים לשני המצבים
- התמיכה במצב כהה מופעלת באמצעות `[data-theme="dark"]`

משתנים עיקריים:
- `--foreground`: צבע טקסט ראשי
- `--background`: צבע רקע ראשי
- `--card-bg`: צבע רקע לכרטיסים
- `--table-text`: צבע טקסט בטבלאות
- `--table-header-bg`: רקע כותרות טבלה
- `--table-row-hover`: צבע שורה בטבלה בעת ריחוף

### 2. שירות ניהול תמה (`ThemeService`)
שירות חדש שנוצר ב-`src/app/core/services/theme.service.ts`:

**פונקציות:**
- `getCurrentTheme()`: מחזיר את התמה הנוכחית
- `setTheme(theme)`: מגדיר תמה (light/dark)
- `toggleTheme()`: מחליף בין תמה בהירה לכהה
- שומר את הבחירה ב-localStorage
- זיהוי העדפת המערכת בטעינה ראשונה

### 3. תיקוני CSS במרכיבים
עודכנו קבצי SCSS של:
- `products.scss`: תמיכה בטבלאות וכרטיסים במצב כהה
- `tools.scss`: תמיכה בטבלאות וכרטיסים במצב כהה
- `dishes.scss`: תמיכה בטבלאות וכרטיסים במצב כהה

כל הטבלאות עודכנו עם:
```scss
td {
  color: var(--table-text);
  h3 {
    color: var(--table-text);
  }
}
```

### 4. מתג תמה בהגדרות
נוסף מתג InputSwitch בדף ההגדרות (`settings.component`):
- אייקונים של שמש וירח
- מעבר חלק בין המצבים
- הודעת הצלחה בעת החלפה

## איך להשתמש?

1. **עבור להגדרות:** לחץ על "הגדרות" בתפריט הראשי
2. **מצא את מתג התמה:** בכרטיס "מראה ותחושה"
3. **החלף מצב:** לחץ על המתג לשינוי בין מצב בהיר לכהה

המערכת תזכור את הבחירה שלך ותטען אותה אוטומטית בפעם הבאה.

## תאימות דפדפנים

התמיכה במצב כהה עובדת בכל הדפדפנים המודרניים:
- Chrome/Edge 76+
- Firefox 67+
- Safari 12.1+

## הערות טכניות

- התמה נשמרת ב-localStorage תחת המפתח `app-theme`
- המערכת מזהה העדפת מצב כהה של המערכת בטעינה ראשונה
- כל השינויים מיושמים באמצעות משתני CSS ללא טעינה מחדש של הדף
